#lang racket
; Este archivo se puede ejecutar en VS Code con la extensión Magic Racket
; usando Ctrl+Alt+N con Code Runner o configurando una tarea personalizada

(define (suma a b)
    (+ a b))

(suma 2 3) ; Llamada a la función suma con 2 y 3 como argumentos